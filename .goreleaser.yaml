version: 2

project_name: binget

builds:
  - builder: zig
    flags:
      - -Doptimize=ReleaseSafe
    targets:
      - x86_64-linux-gnu
      - x86_64-windows-gnu
      - aarch64-linux-gnu
      - aarch64-macos-none
      - x86_64-macos-none

archives:
  - formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}-{{ lower .Os }}-{{ if eq .Arch "amd64" }}x86_64{{ else if eq .Arch "arm64" }}aarch64{{ else }}{{ lower .Arch }}{{ end }}-v{{ .Version }}
    format_overrides:
      - goos: windows
        formats:
          - zip

nfpms:
  - formats:
      - deb
      - rpm
      - apk
    maintainer: "Binget Authors <email@example.com>"
    description: "Binget package manager"
    bindir: /usr/bin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
